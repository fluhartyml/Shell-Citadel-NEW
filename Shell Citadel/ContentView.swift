//
//  ContentView.swift
//  Shell Citadel
//
//  Created by Michael Fluharty on 9/4/26.
//
//  Placeholder. The terminal replaces this at roadmap step 1. For now it shows the
//  build readout, because step 0.1 says the build number must be reachable from the
//  device before anything else is built on top of it.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        AboutView()
    }
}

#Preview {
    ContentView()
}
